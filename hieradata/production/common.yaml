---
lookup_options:
  logrotate::rules:
    merge:
      strategy: deep
      merge_hash_arrays: true
bla::wassup: "yeah boi"
logrotate::rules:
  rob:
    path: '/var/log/rob'
    missingok: true
    ifempty: false
    size: '1k' 
    create: true
